// Ask PAL. what action type should be, the below or just preaction, action
enum ActionType {
  case RunRadar
  case DropRover
	case DropMine
	case SendMissile
  case SendMessage
  case ReceiveMessage
  case SetSheilds
}